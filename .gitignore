# macOS
.DS_Store

# build outputs (root-only; the unanchored form also matches internal/freeagent/)
/freeagent
/freeagent-ro

# Go
/bin/
/dist/
*.exe
*.dll
*.so
*.dylib

# e2e harness — local sandbox token files copied here are never committed
/.e2e/
